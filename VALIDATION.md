# 등급 시뮬레이터 — 예상 시나리오 검증 리포트

검증일: 2026-07-13 · 대상: `index.html` (3학년 등급 시뮬레이터 · 1학기)

## 요약

| 구분 | 결과 |
|---|---|
| 단위·시나리오 테스트 (Node) | **74 / 74 통과** |
| 브라우저 E2E 스모크 (Chromium) | **통과 · JS 오류 0건** |
| 발견된 로직 결함 | **없음** |

## 검증 방법

HTML 내 핵심 로직(SimCore: 파서 4종 + 등급 엔진 + 통계 + GPA)은 Node 호환으로 작성되어 있어
그대로 추출해 `test/scenarios.test.js`로 검증했고, 실제 사용 흐름은 Chromium에서
가상 나이스 xlsx(1·2차 학급별 일람표, 생기부)를 생성해 드래그앤드롭으로 적재하는
E2E 스모크 테스트(`test/e2e.test.js`)로 확인했다.

```bash
cd test
node scenarios.test.js          # 단위·시나리오 74건
npm install playwright-core     # E2E 실행 시 1회
node e2e.test.js                # 브라우저 종단간 스모크
```

## 시나리오별 결과 (14개 영역, 74건)

1. **교육과정·산출방식 자동 분류** — 2015 일반선택→9등급, 진로선택·체예→성취도,
   2022 공통→5등급, 융합선택→성취도. 공백·로마자(Ⅱ) 정규화, 동명 과목 2015 우선,
   미등재 과목 "인식 실패" 라벨까지 정상. ✓
2. **교과영역 판별** — 나이스 교과 우선, 없으면 과목명 추론 (경제 수학: 교과=사회→사회 / 교과 없음→수학). ✓
3. **9등급 누적비율 반올림** — n=100 컷 `[4,11,23,40,60,77,89,96,100]`,
   n=33 컷 `[1,4,8,13,20,25,29,32,33]` (40위→4등급, 41위→5등급 등 경계 정확). ✓
4. **경계 동점 중간석차백분율** — 3~6위 4명 동점(1·2등급 경계) → 중간석차 4.5% → 전원 2등급,
   경계 안 동점은 그대로, 중간석차 정확히 4.0%는 1등급 인정(≤ 판정),
   1~60위 대량 동점 → 30.5% → 4등급 관통 처리. ✓
5. **5등급제(2022)** — 누적 10/34/66/90/100%, n=50 컷 `[5,17,33,45,50]`. ✓
6. **수강자수 수기 보정** — 입력 10명·수강 25명(결시 15명) 시 25명 기준 컷 적용. ✓
7. **기술통계** — 모표준편차(√(Σ(x−μ)²/n)) 기준. ✓
8. **학급별 일람표 파서** — 다중 페이지 블록 병합, `교과:과목(단위)` 헤더, 결시 빈칸 제외,
   소수점 점수, 응시생수/학과응시생수 푸터, 학번 보존. ✓
9. **교과목별 일람표 파서** — 반×번호 매트릭스, 2차 시험·과목·단위 인식, 반별 응시생수. ✓
10. **생기부 파서** — `원점수/평균(표준편차)` 분해, `성취도(수강자수)`, 번호·학년·학기 forward-fill,
    원점수만 있는 행, P(이수) 과목 등급 제외. ✓
11. **명렬표 파서** — 반 열 사용 / 반 열 없으면 제목의 "N반" 사용. ✓
12. **파일 종류 자동 판별** — 제목 기반 4종 판별 + 제목 없을 때 구조 기반 폴백, 무관 파일 거부. ✓
13. **평균등급(GPA)** — 학점 가중, 성취도·0학점 과목 제외, 학기별·누적 일치. ✓
14. **1·2차 합산 종단 시나리오** — 40:60 가중 합산 후 등급 산출.
    n=6이면 1등급 정원 0명(1위→2등급), 9등급 정원 0명(꼴찌→8등급) — 반올림 규정상 올바른 동작. ✓

## E2E 스모크 확인 사항

- 1차·2차 일람표 + 생기부 3파일 동시 적재 → 파일 태그·검증표 정상 표시
- 미적분: 2차 존재 시 자동 "1·2차 합산 50:50" 전환, 화법과 작문: 1차 기준, 운동과 건강: 성취도(80/60) 자동 분류
- 학과응시생수 대조 "일치" 배지 표시
- 학생 조회(학번 3101): 3-1 예상 평균등급 2.00 / 기존 누적 1.75 / 최종 누적 1.83, T점수·내신 추이·교과영역 분석 렌더링
- 과목별 등급표: 경계 동점 73.5점 2명 → 중간석차 45.8% → 5등급 배지·경고문 표시 (수기 계산과 일치)
- 콘솔·페이지 JS 오류 0건

## 검증 중 수정한 사항

- **로직 수정 없음.** 원본 로직 결함은 발견되지 않았다.
- 배포본(`index.html`)은 오프라인·CSP 환경에서 동작하도록 xlsx 라이브러리(0.18.5)를 인라인하고
  Pretendard 웹폰트 CDN 링크를 제거(시스템 폰트 폴백)했다. 로직은 원본과 동일하다.

## 참고 (한계)

- 지필 점수만으로 석차를 가정하므로 수행평가 반영 실제 등급과 다를 수 있음 (앱 안내문과 동일)
- 저장은 브라우저 localStorage 기반 — 기기·브라우저 간 공유되지 않음
